#include "sam-system.h"
#include "factor_impl/anchor.h"
#include "factor_impl/linear-translation.h"

int main (int argc, char *argv[])
{
  // auto syst = SAM::SamSystem<factor::anchor, factor::linear-translation>();
  
  // receive the measurement from stdin (as a string that can be converted in a C++ container)


  return 0;
}
